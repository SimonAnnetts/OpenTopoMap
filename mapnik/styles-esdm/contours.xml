<Style name="contours">
	<!-- 100 m -->
	<Rule>
		&maxscale_zoom11;
		&minscale_zoom11;
		<Filter>([ele] % 100 = 0) and ([ele] != 0)</Filter>
		<LineSymbolizer stroke="&color_contours;" stroke-width="0.7" />
	</Rule>
	<Rule>
		&maxscale_zoom12;
		&minscale_zoom12;
		<Filter>([ele] % 100 = 0) and ([ele] != 0)</Filter>
		<LineSymbolizer stroke="&color_contours;" stroke-width="0.8" />
	</Rule>
	<Rule>
		&maxscale_zoom13;
		&minscale_zoom13;
		<Filter>([ele] % 100 = 0) and ([ele] != 0)</Filter>
		<LineSymbolizer stroke="&color_contours;" stroke-width="0.9"  />
	</Rule>
	<Rule>
		&maxscale_zoom14;
		&minscale_zoom17;
		<Filter>([ele] % 100 = 0) and ([ele] != 0)</Filter>
		<LineSymbolizer stroke="&color_contours;" stroke-width="1.0" smooth="0.5" />
	</Rule>
	<Rule>
		&maxscale_zoom13;
		&minscale_zoom14;
		<Filter>([ele] % 100 = 0) and ([ele] != 0)</Filter>
		<TextSymbolizer fontset-name="sans-book" size="11" fill="&color_contours;" upright="left" halo-radius="1" halo-fill="rgba(255,255,255,1)" halo-comp-op="soft-light" placement="line" max-char-angle-delta="10" label-position-tolerance="100" minimum-path-length="200" spacing="200">[ele]</TextSymbolizer>
	</Rule>

	<!-- 50 m -->
	 <Rule>
		&maxscale_zoom11;
		&minscale_zoom11;
		<Filter>([ele] % 100 != 0) and ([ele] % 50 = 0) and ([ele] != 0)</Filter>
		<LineSymbolizer stroke="&color_contours;" stroke-width="0.6" />
	</Rule>
	<Rule>
		&maxscale_zoom13;
		&minscale_zoom13;
		<Filter>([ele] % 100 != 0) and ([ele] % 50 = 0) and ([ele] != 0)</Filter>
		<LineSymbolizer stroke="&color_contours;" stroke-width="0.6"  />
	</Rule>
	<Rule>
		&maxscale_zoom14;
		&minscale_zoom17;
		<Filter>([ele] % 100 != 0) and ([ele] % 50 = 0) and ([ele] != 0)</Filter>
		<LineSymbolizer stroke="&color_contours;" stroke-width="0.6" smooth="0.5" />
	</Rule>
	<Rule>
		&maxscale_zoom14;
		&minscale_zoom14;
		<Filter>([ele] % 100 != 0) and ([ele] % 50 = 0) and ([ele] != 0)</Filter>
		<TextSymbolizer fontset-name="sans-book" size="11" fill="&color_contours;" upright="left" halo-radius="1" halo-fill="rgba(255,255,255,0.8)" halo-comp-op="soft-light" placement="line" max-char-angle-delta="10" label-position-tolerance="100" minimum-path-length="200" spacing="200">[ele]</TextSymbolizer>
	</Rule>

	<!-- 20 m -->
	 <Rule>
		&maxscale_zoom12;
		&minscale_zoom12;
		<Filter>([ele] % 100 != 0) and ([ele] % 20 = 0) and ([ele] != 0)</Filter>
		<LineSymbolizer stroke="&color_contours;" stroke-width="0.6" smooth="0.5" />
	</Rule>

	<!-- 10 m -->
	<Rule>
		&maxscale_zoom13;
		&minscale_zoom17;
		<Filter>([ele] % 50 != 0) and ([ele] % 10 = 0) and ([ele] != 0)</Filter>
		<LineSymbolizer stroke="&color_contours;" stroke-width="0.6" smooth="0.5" />
	</Rule>
	<Rule>
		&maxscale_zoom15;
		&minscale_zoom17;
		<Filter>([ele] % 10 = 0) and ([ele] != 0)</Filter>
		<TextSymbolizer fontset-name="sans-book" size="11" fill="&color_contours;" upright="left" halo-radius="1" halo-fill="rgba(255,255,255,0.3)" placement="line" max-char-angle-delta="10" label-position-tolerance="100" minimum-path-length="300" spacing="800">[ele]</TextSymbolizer>
	</Rule>
</Style>
